# State: ai-benchmark-deployment-gap

status: in_progress
current_milestone: 3
attempt: 1

## Next action

The fresh-eyes evaluator returned **FAIL** on M3 attempt 0 (`verdicts/2026-06-28-m3-attempt0.md`): two of four done-criteria not met. **(1) Rule not reproducible** — the stated combination rule (§1.1 Step B / §1.2: per-endpoint maximum across channels, "low end is the max of the channels' low ends; high end is the max of the channels' high ends") does not reproduce the two SWE-bench rows: SWE/GPT-5 inputs (contam 0, priv 26.1, horizon 49.9) give per-endpoint max 49.9–49.9 but the table shows 26.1–49.9; SWE/Opus-4.1 (0, 0, 49.9) give 49.9–49.9 but the table shows 0–49.9. The artifact's own inline `max(26.1, 49.9) ⇒ 26.1–49.9` and `max(0,0,49.9) ⇒ 0–49.9` are mathematically false (max returns a scalar, not a band) — the table is silently using a *span-between-channels* rule, not the written per-endpoint-max rule. MMLU and the GSM8K worked example reproduce, but only because their dominant channel already carries an internal range. **(3) No 'usable' row** — criterion 3 verbatim requires at least one benchmark classified 'usable'; the artifact's usable tier is explicitly "defined and shown empty," which does not satisfy a criterion mandating a usable instance. Criteria 2 and 4 met. This is attempt 0's FAIL → `attempt` incremented to **1** (still < 2, so not blocked). The next run matches **decision-table row 2** (`status: in_progress`): re-run the **generator** subagent on M3, passing in the FAIL verdict path (`verdicts/2026-06-28-m3-attempt0.md`) so it can (a) reconcile the written rule with the table — pick per-endpoint-max OR a span rule and re-derive all six rows consistently, fixing the false `max(...)` inline arithmetic — and (b) produce at least one legitimately 'usable' row with cited M2 components (natural candidates per the verdict: same-model frontier readings — Opus-4.1 SWE public→private flat at 0 pp, or GPQA same-model contamination ~0.5 pp). It overwrites/replaces the M3 artifact, then sets `status: awaiting_review`. **This is the last attempt: a second FAIL on M3 → `status: blocked`.**

## Run log
<!-- newest first, one line per run -->
- 2026-06-28 — evaluator ran (decision-table row 3 — `status: awaiting_review`, M3, attempt 0). Fresh-eyes evaluator subagent given ONLY the four M3 done-criteria (quoted), the M3 artifact path (`artifacts/2026-06-28-m3-procurement-discount.md`), the M2 components artifact path (read solely to check the traceability/reproducibility of criterion 1), and a verdict output path — no generator reasoning, no run history, no orchestrator opinion. **VERDICT: FAIL** (`verdicts/2026-06-28-m3-attempt0.md`). Criterion 1 (rule reproducible) NOT MET: evaluator re-applied the written per-endpoint-max rule to the M2 rows and reproduced MMLU (16–33) but NOT the two SWE-bench rows — SWE/GPT-5 (contam 0, priv 26.1, horizon 49.9) → per-endpoint max 49.9–49.9 ≠ table's 26.1–49.9; SWE/Opus-4.1 (0, 0, 49.9) → 49.9–49.9 ≠ table's 0–49.9; the artifact's inline `max(26.1,49.9)⇒26.1–49.9` is false (max yields a scalar), so the table silently uses a span-between-channels rule, not the written rule. Criterion 3 (≥1 'usable') NOT MET: usable tier explicitly empty; the criterion as written mandates a usable instance, and honest reporting of an empty tier does not satisfy it. Criteria 2 (every row carries band/classification/confidence; ascending-by-Discount_high monotonic ranking) and 4 (full GSM8K M1→M2→discount worked example) MET. Routed FAIL → `attempt` 0→1 (still < 2, not blocked), `status: in_progress`. **NB the watch point flagged for the empty-usable-tier question was adjudicated by the evaluator as a FAIL, not pre-decided by the orchestrator.** Next run: generator re-runs M3 (row 2) with the FAIL verdict passed in; a second FAIL would block.
- 2026-06-28 — generator ran (decision-table row 2 — `status: in_progress`, M3, attempt 0; no prior FAIL verdict to pass in). No `.claude/skills/` entry matches M3 (library holds only README), so it ran freeform per spec ("Skill: none — freeform"). Fresh generator subagent read `problem.md`, `spec.md`, the M1 dossier and the M2 gap-component table and wrote one artifact, `artifacts/2026-06-28-m3-procurement-discount.md`: a methods preamble fixing a single uniform combination rule — convert M2's contamination fraction to pp via `P_contam = c × H_ref` (H_ref = each benchmark's frontier headline, an M1 row), pass the two Δpp channels through unchanged, then take the **per-endpoint max across the three channels** (max not sum, because M2 Lim#1 says the channels overlap and must not stack); ranges propagate endpoint-wise; insufficient-evidence channels contribute 0 (so a computed discount is a known lower bound); classification keyed on `Discount_high` (≤10 pp `usable`, >10–35 `discount-heavily`, >35 `noise`); rows ranked ascending by `Discount_high`. Result (6 rows, ascending): GPQA 0.5–14.9 pp (discount-heavily, contamination, low conf) → MMLU 16–33 (discount-heavily, private-gap, med) → SWE-bench/Opus-4.1 0–49.9 (noise, horizon, med) → SWE-bench/GPT-5 26.1–49.9 (noise, horizon, med) → GSM8K 28.5–62.7 (noise, horizon, med) → MATH 16–96 (noise, contamination, low). Worked example: GSM8K walked end to end (M1 R22/R23/R24/R25 → M2 components → 28.5–62.7 pp → noise). Generator's headline finding: the long-horizon/robustness channel — not contamination — dominates the haircut for coding/grade-school-math, and the **`usable` (≤10 pp) tier came out empty** (no public benchmark survives a worst-case procurement test with a small haircut); generator flagged in Limitations the counter-tension that on *same-model-frontier* evidence alone (Opus-4.1 flat public→private, GPQA ~3% leakage) the haircut would be near zero. Every number traces to M2 (→ M1 rows); no new external figure; every [CROSS-MODEL]/[LOW-CONF n=1]/[PROXY] input carries confidence ≤ med. NB for next run: the empty `usable` tier sits against M3 done-criterion 3's "at least one ... 'usable'" — left for the fresh-eyes evaluator to adjudicate, not pre-judged here. Set `status: awaiting_review`. Next run: evaluator (row 3) writes a verdict.
- 2026-06-28 — evaluator ran (decision-table row 3 — `status: awaiting_review`, M2, attempt 0). Fresh-eyes evaluator subagent given ONLY the four M2 done-criteria (quoted), the artifact path (`artifacts/2026-06-28-m2-gap-components.md`), the M1 dossier path (needed solely to verify the traceability criterion), and a verdict output path — no generator reasoning, no run history. **VERDICT: PASS** (`verdicts/2026-06-28-m2-attempt0.md`), all four criteria met: (1) every numeric cell is either a low–high range with M1 row IDs or the literal token "insufficient evidence"; (2) the three formulas are stated once in the preamble and applied identically — the evaluator independently followed the MMLU contamination cell to M1 rows R18/R19, re-applied c=(H−D_clean)/H, and reproduced 0.166–0.186, and also reproduced GSM8K horizon (28.5–62.7 from R24) and SWE public→private (0–26.1 from R7/R8); (3) every single-data-point cell carries a low-confidence flag ([LOW-CONF n=1] / [CROSS-MODEL] / [PROXY]); (4) exactly 4 of 5 benchmarks (SWE, MMLU, GSM8K, MATH) carry ≥2 of 3 populated components. Three citation spot-checks (arXiv 2412.15194 MMLU-CF, 2410.05229 GSM-NoOp, 2509.16941 SWE-Bench Pro) supported their claims; base rubric (Sourced/Current/Adversarial/Format) all pass. Routed PASS → advanced `current_milestone` 2→3, reset `attempt: 0`, set `status: in_progress`. Next run: generator builds M3 (row 2).
- 2026-06-28 — generator ran (decision-table row 2 — `status: in_progress`, M2, attempt 0; no prior FAIL verdict to pass in). No `.claude/skills/` entry matches M2 (library holds only README), so it ran freeform per spec ("Skill: none — freeform"). Fresh generator subagent read `problem.md`, `spec.md`, and the M1 dossier (`artifacts/2026-06-28-m1-evidence-dossier.md`) and wrote one artifact, `artifacts/2026-06-28-m2-gap-components.md`: a methods preamble stating three uniform formulas (contamination fraction c=(H−D_clean)/H; public→private drop Δpp=H_public−D_private; short→long-horizon drop Δpp=H_short−D_long) each with one worked example, then a per-benchmark computation table plus a per-cell re-derivable arithmetic section, all inputs cited by M1 row ID (R1–R29), no new external numbers. Populated components: SWE-bench (public→private 0–26.1 pp [genuinely model-dependent, M1 G1], horizon ≈49.9 pp [LOW-CONF n=1]), GPQA (contam 0.005–0.166 only), MMLU (contam 0.166–0.186, public→private 16–33), GSM8K (contam 0.067–0.107, horizon 28.5–62.7), MATH (contam 0.01–0.96 [PROXY], public→private 16–33 [PROXY]) — 4 of 5 benchmarks carry ≥2 of 3 components (SWE, MMLU, GSM8K, MATH); every gap uses the literal token "insufficient evidence"; n=1/cross-model/proxy cells flagged. Notable judgement calls: SWE contamination marked insufficient because R3's flawed-test finding deflates the model rather than inflating the headline (wrong sign for formula i); field/PR-acceptance R11 deliberately excluded from all three columns (matches none of the three formula types). Set `status: awaiting_review`. Next run: evaluator (row 3) writes a verdict.
- 2026-06-28 — evaluator ran (decision-table row 3 — `status: awaiting_review`, M1, attempt 0). Fresh-eyes evaluator subagent given ONLY the four M1 done-criteria (quoted), the artifact path, and a verdict output path — no generator reasoning or run history. It read the dossier, independently read the brief's "State of the art" paragraph for criterion 4, and WebFetch/WebSearch spot-checked 9 primary-source URLs spread across all five families and both source types. **VERDICT: PASS** (`verdicts/2026-06-28-m1-attempt0.md`), all four criteria met: (1) five benchmarks each ≥3 rows — SWE-bench 11, GPQA 4, MMLU 6, GSM8K 4, MATH 4; (2) every sampled primary URL resolved to the correctly-named study with matching numbers, the one weak-provenance row R11 (presenc.ai PR-acceptance proxy) flagged in-artifact and not a blog-of-unnamed-study; (3) ≥1 deflationary row per family; (4) all six brief SOTA claims represented and traced to original sources — the evaluator independently re-verified the dossier's corrections of the brief's mis-citations (2601.04301 is a MATH-only study; the GSM8K figure traces to GSM1k at 8%, the leakage range to 2310.17589) against the primary sources and confirmed them accurate. Routed PASS → advanced `current_milestone` 1→2, reset `attempt: 0`, set `status: in_progress`. Next run: generator builds M2 (row 2).
- 2026-06-28 — generator ran (decision-table row 2 — `status: in_progress`, M1, attempt 0; no prior FAIL verdict to pass in). No `.claude/skills/` entry matches M1 (library holds only README), so the milestone ran freeform. Fresh generator subagent read `problem.md` + `spec.md` and wrote one artifact, `artifacts/2026-06-28-m1-evidence-dossier.md`: a 29-row source-keyed evidence table (R1–R29) across five core benchmark families each with ≥3 rows (SWE-bench/Verified — incl. SWE-Bench Pro and SWE-EVO deflationary variants — GPQA, MMLU, GSM8K, MATH), every row carrying a primary-source URL + date verified against the source during the run, every family carrying ≥1 deflationary (contamination-adjusted / private-holdout / long-horizon) row, plus a coverage-check mapping each brief "State of the art" claim to a row traced to its original arXiv/vendor source, and a "Gaps & contested points" section (G1–G8). Notable: verification overturned several brief numbers, which the dossier records with the *verified* figures rather than reproducing the brief — arXiv 2601.04301 studies MATH (not the GSM8K "13%"/"1–45%" leakage claims it was tagged with; those trace to the GSM1k paper at 8%, and to Li/Guerin/Lin 2310.17589); the SWE-Bench Pro "Opus 4.1 22.7%→17.8%" pairing is wrong (Opus is ~flat 17.8%; the real public→private collapse is GPT-5 41.8%→15.7%); the Presenc "50–60% below" is a 35–50% vs 74–78% gap; and genuine counter-evidence was logged (GPQA looks contamination-robust; frontier models barely move on numeric-only GSM8K perturbation). All four M1 done-criteria reported met. Set `status: awaiting_review`. Next run: evaluator (row 3) writes a verdict.
- 2026-06-28 — planner ran (decision-table row 1 — no `spec.md`). Fresh planner subagent read `problem.md` and wrote `spec.md` with 4 build-on-each-other milestones for the deployment-validity ledger: M1 evidence dossier (source-keyed table, every row a working primary-source URL + date, ≥5 benchmarks × ≥3 rows, ≥1 deflationary point per benchmark); M2 quantify the three gap components (contamination fraction, public→private drop, short→long-horizon drop) with low–high ranges, uniform stated formulas, and an explicit "insufficient evidence" token; M3 derive the per-benchmark procurement discount via an explicit reproducible combination rule + classification {usable | discount-heavily | noise} + worked example; M4 buyer-facing brief to CAISI/NIST + GSA with ≥5 limitations and per-benchmark falsification tests, barred from introducing any number absent from M1–M3. All milestones tagged "none — freeform" (`.claude/skills/` holds only README); planner flagged `evidence-dossier`/`source-synthesis` as future skill-extraction candidates if the loop revisits this pattern. Set `status: in_progress`, `current_milestone: 1`. Next run: generator builds M1.
- 2026-06-28 — scout ran (decision-table row 5 — no active problem; all others published or dropped). Fresh scout subagent read `PREFERENCES.md` and the covered `problems/` set, built a 5-candidate slate spanning both lanes (3 frontier, 1 welfare, 1 dropped) and three shapes, scored against preferences, and wrote the winner's brief to `problems/ai-benchmark-deployment-gap/problem.md`. **Winner: the AI-benchmark-to-deployment gap** (payoff: frontier/AI — operator's #1 lane; shape: **the teardown**, not yet published — breaks the overdue-fix monoculture; score 9/10). The thesis: public leaderboards (SWE-bench, GPQA, math sets) systematically overstate what frontier models do on real, unseen work, and no one has published a per-benchmark "procurement discount." Deciding factors: top lane + unpublished shape + a live named decision-maker (CAISI/NIST + GSA writing the federal AI-procurement eval standard right now, March–May 2026 MOU + five-lab pre-deployment agreements), quantified stakes (95% of enterprise GenAI pilots no measurable P&L; ~40% of agentic projects to be cancelled by 2027; scores collapsing 23%→15% on private codebases; 50–60% benchmark-to-PR-acceptance gap), a sharp falsifiable inversion (the numbers pricing frontier AI are partly recall/contamination, not capability), and a buildable desk wedge (a reproducible per-benchmark deployment-validity ledger CAISI itself says does not exist). Slate (payoff | shape | score | deciding preference): **AI benchmark-to-deployment gap** [WINNER] frontier/AI | teardown | 9 | top lane + unpublished shape + live decision + inversion; megaconstellation orbital-traffic carrying capacity | frontier/space | measurement | 7 | strong but overlaps published uncontrolled-reentry domain, lost on novelty; thermosphere density / reentry-prediction error | frontier/space | measurement | 6 | weaker named-decision anchor, more academic; snakebite antivenom access gap | welfare/global-health | overdue-fix dossier | 6 | solid welfare anchor but over-represented shape, kept as required welfare candidate; enhanced geothermal (Fervo) stranded scale-up | frontier/energy | forecast | DROPPED | hard filter "actually being solved" ($880M+ 2025–26 financing, commercial plants underway). Scout wrote only `problem.md`; orchestrator created this `state.md`. Set `status: no_spec`. Next run: planner writes `spec.md`.
