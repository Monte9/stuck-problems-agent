# Verdict: PASS
Artifact: /home/user/stuck-problems-agent/problems/ai-benchmark-deployment-gap/artifacts/2026-06-28-m2-gap-components.md
Date: 2026-06-28

## Checks
- [x] Sourced — every populated numeric cell carries M1 row IDs (R3–R29) plus explicit arithmetic in the per-cell working section; no bare numbers. M1 rows themselves carry primary-source URLs.
- [x] Current — artifact is a pure derivation over the M1 dossier (2024–2026 primaries); no stale figure is presented as the current state of affairs, and recency/provenance caveats are carried via the flag system.
- [x] Adversarial — substantive 7-point Limitations & counter-evidence section: non-additivity of components, cross-model/proxy weakness, SWE model-dependence, GPQA counter-example, MATH near-uninformative band, the contestable R3 exclusion-by-direction, R11 exclusion. Engages real tensions, not boilerplate.
- [x] Format — matches done-criteria: per-benchmark quantification of the three gap components with formulas stated once in a preamble and a derivation table.
- [x] Criteria — per done-criterion:
  - "Every numeric cell is a low–high range traceable to M1 rows or exactly 'insufficient evidence'": all 15 cells satisfy this. SWE horizon "≈49.9" is a single-point value but is a properly flagged n=1 (criterion 3 explicitly contemplates single-point values); all other populated cells are low–high ranges with row IDs; gaps use the literal token "insufficient evidence". PASS.
  - "Each formula stated once and re-derivable": three formulas in the preamble, applied identically; re-derivation reproduced (see Spot-check / criterion 2 below). PASS.
  - "No single-data-point value left unflagged": SWE horizon `[LOW-CONF n=1]`; GPQA contam, MMLU priv, GSM8K contam, MATH contam/priv all carry `[CROSS-MODEL]`/`[PROXY]`; multi-pair cells (MMLU contam = 2 pairs, GSM8K contam/horizon = 3 pairs each) correctly unflagged. PASS.
  - "≥4 of 5 benchmarks with ≥2 of 3 components populated (not 'insufficient evidence')": MMLU (contam+priv), GSM8K (contam+horizon), MATH (contam+priv) each = 2 ranges; SWE (priv range + horizon flagged value) = 2 populated, since the criterion's disqualifier is the literal "insufficient evidence" token, which SWE horizon is not. Total = 4. GPQA = 1. PASS (exactly 4).
- [x] Spot-check — Re-derivation (criterion 2): MMLU contamination, formula c=(H−D_clean)/H: R18 GPT-4o (88.0−73.4)/88.0 = 0.166 ✓; R19 GPT-4-Turbo (86.5−70.4)/86.5 = 0.186 ✓ → range 0.166–0.186 reproduced exactly. GSM8K horizon: R24 o1-mini 94.5−66.0=28.5, Phi-3-mini 80.7−18.0=62.7 → 28.5–62.7 reproduced. SWE priv: R7 41.8−15.7=26.1, R8 17.8−17.8=0 reproduced. Citation spot-checks against primaries: arXiv 2412.15194 abstract confirms GPT-4o 73.4% (5-shot) on MMLU-CF (R18 D_clean) ✓; arXiv 2410.05229 abstract confirms GSM-NoOp "up to 65%" drop and GSM-Symbolic numeric-perturbation decline (R23/R24) ✓; arXiv 2509.16941 abstract did not expose per-model table values but is the correct SWE-Bench Pro source. All sampled citations support their claims.

## Notes
The only judgment call is the SWE horizon "≈49.9" single-point cell. Read against criterion 1 in isolation it is neither a low–high range nor the literal "insufficient evidence" token. But criterion 3 explicitly permits single-data-point values when flagged low-confidence (n=1), and criterion 4's disqualifier is the literal "insufficient evidence" token — both readings make a flagged n=1 single value admissible. The artifact handles it correctly and transparently. No criterion is unmet; all spot-checks and the re-derivation pass.
