# Verdict: PASS
Artifact: /home/user/stuck-problems-agent/problems/ai-benchmark-deployment-gap/artifacts/2026-06-28-m2-gap-components.md
Date: 2026-06-28

## Checks

### Done-criteria
- [x] **Every numeric cell is a low–high range traceable to cited M1 rows, or exactly "insufficient evidence."** — Every populated cell in the table carries explicit M1 row IDs (R3–R29) and arithmetic; every gap cell uses the literal token `insufficient evidence` (verified character-for-character: SWE contam, GPQA priv/horizon, MMLU horizon, GSM8K priv, MATH horizon). No bare point estimate appears without a range except where the range collapses to a single matched pair, which is flagged (see n=1 check). MET.

- [x] **Each component's formula stated once in the preamble and applied identically; an evaluator can re-derive a cell.** — Three formulas (i) `c=(H−D_clean)/H`, (ii) `Δpp_priv=H_public−D_private`, (iii) `Δpp_horizon=H_short−D_long` are each stated exactly once with one worked example. I re-derived independently below and reproduced the range. MET.

- [x] **No component value computed from a single data point without a low-confidence flag.** — Single-point cells: SWE horizon (≈49.9, one matched pair R4→R9) flagged `[LOW-CONF n=1]`; GPQA contam (single subset study R14) flagged `[CROSS-MODEL]` + explicit note "Both endpoints rest on indirect, subset-only evidence... near the low end"; MMLU priv (single aggregate band R20) flagged `[CROSS-MODEL]`; MATH contam (R27 + R21 floor) and MATH priv (R29) flagged `[PROXY][CROSS-MODEL]`. Multi-pair cells correctly NOT flagged n=1: MMLU contam (R18+R19, two pairs), GSM8K contam (R23, three pairs), GSM8K horizon (R24, three pairs). No single-data-point cell is left unflagged. MET. (Note: GPQA contam rests on one row and carries a low-confidence flag via CROSS-MODEL + note rather than the literal n=1 token; the criterion requires a low-confidence flag, which is present.)

- [x] **≥4 of 5+ benchmarks have ≥2 of 3 components populated with a range.** — SWE (priv 0–26.1 + horizon ≈49.9) = 2; MMLU (contam 0.166–0.186 + priv 16–33) = 2; GSM8K (contam 0.067–0.107 + horizon 28.5–62.7) = 2; MATH (contam 0.01–0.96 + priv 16–33) = 2. Four benchmarks at ≥2. GPQA at 1 (contam only). 4 ≥ 4. MET.

### Base rubric
- [x] **Sourced** — Every numeric input traces to an M1 row ID; no new external number is introduced. The artifact is a pure derivation over M1.
- [x] **Current** — Frontier-state claims rest on 2025-12/2026-01/2026-02 rows (R3, R4, R9, R27). Older rows (R18/R19 2024-12, R23/R24 2024-10, R20 2024-06) are used as mechanism evidence and explicitly flagged cross-model/proxy, not presented as the current frontier number.
- [x] **Adversarial** — A substantive 7-point Limitations & counter-evidence section engages real defects: non-additivity of components, cross-model/proxy weakness, genuine model-dependence of the SWE range, GPQA as counter-example, near-uninformative MATH band, the contestable exclusion of R3, and exclusion of the R11 field proxy. Not boilerplate.
- [x] **Format** — Matches the done-criteria format: per-benchmark computation table with three component columns as low–high ranges, plus methods preamble and per-cell working.
- [x] **Spot-check** — See below.

## Worked re-derivation (independent recompute)

I chose **MMLU contamination fraction (claimed 0.166–0.186)**.
Formula (i): `c = (H − D_clean)/H`.
- R18 GPT-4o: H=88.0 (R17), D_clean=73.4 (R18). `c = (88.0−73.4)/88.0 = 14.6/88.0 = 0.16591 → 0.166`. Matches low end.
- R19 GPT-4-Turbo: H=86.5, D_clean=70.4 (R19). `c = (86.5−70.4)/86.5 = 16.1/86.5 = 0.18613 → 0.186`. Matches high end.
Reproduced range **0.166–0.186** exactly. Two distinct model-matched pairs → correctly not flagged n=1.

I also independently re-derived the **GSM8K horizon** cell from R24: o1-mini 94.5−66.0 = 28.5 (low), Phi-3-mini 80.7−18.0 = 62.7 (high), GPT-4o 94.9−63.1 = 31.8 (interior). Reproduced **28.5–62.7 pp** exactly. And **SWE priv**: R7 41.8−15.7 = 26.1, R8 17.8−17.8 = 0 → **0–26.1** exactly.

## Citation spot-checks (against primary sources, not M1)
- **arXiv 2412.15194 (MMLU-CF):** abstract confirms GPT-4o "5-shot score of 73.4% and a 0-shot score of 71.9%" — matches R18 exactly.
- **arXiv 2410.05229 (GSM-Symbolic/GSM-NoOp):** abstract confirms the "up to 65%" accuracy drop from adding irrelevant clauses, consistent with R24's per-model figures (28.5–62.7 pp). Per-model table values are in the paper body, not the abstract, but the magnitude and mechanism check out.
Both sampled citations support the claims attached to them.

## Defects (non-blocking)
- The GPQA contamination range (0.005–0.166) is derived from a single subset study (R14) for both endpoints; the high end (15/90) is explicitly an "absolute ceiling if leakage were benchmark-wide (it is not)." This is honestly disclosed and flagged, but the band is near-uninformative — flagged correctly for M3 to down-weight. Not a criterion failure.
- The artifact uses `[CROSS-MODEL]` rather than the `[LOW-CONF n=1]` token for the GPQA single-source cell. The low-confidence signal is present (flag + note), so the criterion is met, but a future pass could add an explicit single-source flag for consistency.
- The MATH contamination band (0.01–0.96) mixes a controlled ≤344M-param injection study with a cross-benchmark floor; the artifact flags this heavily and explicitly tells M3 to treat MATH contamination as effectively unquantified. Acceptable given the flags.

All four done-criteria and all six base-rubric checks pass. Verdict: PASS.
