# Verdict: PASS
Artifact: /home/user/stuck-problems-agent/problems/ai-benchmark-deployment-gap/artifacts/2026-06-28-m3-procurement-discount-attempt1.md
Date: 2026-06-28

## Checks
- [x] Sourced — Every discount is a function of M2 component ranges (cited per row) which trace to M1 rows R1–R29. No new external number is introduced; H_ref values (§1.7) are each cited M1 rows. The one proxy figure (MATH H_ref=100) is flagged `[PROXY]`.
- [x] Current — State-of-affairs (2026 frontier procurement) claims rest on 2025–2026 primaries (R3 2026-02, R4/R9 2025-12, R6–R8 2025-09, R27 2026-01). Older sources (2021 MMLU, 2023 GPQA) are used only as benchmark definitions/historical context, not as current state.
- [x] Adversarial — §5 has 10 substantive limitations, including #2 conceding the single `usable` row depends entirely on a contestable scoping decision and #4 admitting the substantive picture is unchanged from attempt-0. Genuinely self-critical, not boilerplate.
- [x] Format — Markdown ranked procurement-discount table with explicit combination rule and worked examples, matching the milestone's deliverable.
- [x] Criteria — all four done-criteria pass (per-criterion below).
- [x] Spot-check — Sampled 3 citations: (a) MMLU-CF arXiv 2412.15194 confirmed GPT-4o 73.4% 5-shot / 71.9% 0-shot vs 88.0% → c=14.6/88=0.166 matches R18 and the artifact's MMLU contam input; (b) GSM-Symbolic/GSM-NoOp arXiv 2410.05229 abstract confirms "up to 65%" drop on irrelevant-clause perturbation, consistent with the horizon channel; (c) SWE-Bench Pro arXiv 2509.16941 abstract confirms the paper/benchmark (table-level numbers in body, not abstract). No misrepresentation found.

### Done-criteria detail
1. Rule explicit & reproducible — PASS. §1.1 states formula (P_contam = c×H_ref, pp pass-through for priv/horizon, per-endpoint max), units, range propagation (§1.2), insufficient-evidence handling (§1.3), scoping (§1.8). Independently re-derived three rows from M2 cells: GSM8K [0.067,0.107]×95=[6.4,10.2], max with horizon [28.5,62.7] → 28.5–62.7 ✓; MMLU [0.166,0.186]×88=[14.6,16.4] max with priv [16,33] → 16–33 ✓; GPQA [0.005,0.166]×90=[0.5,14.9] ✓. All match the table.
2. Every row has range+classification+confidence; monotonic ranking — PASS. All 7 rows carry a band, class, and confidence label. Sorted ascending by Discount_high: 0, 14.9, 33, 49.9, 49.9, 62.7, 96 (non-decreasing); the single tie (ranks 4/5 at 49.9) broken by ascending Discount_low (0 < 26.1). Key stated in §1.6, no exception.
3. ≥1 noise/discount-heavily AND ≥1 usable, each citing M2 components — PASS. `usable`: SWE-bench Pro × Opus 4.1 (rank 1, 0–0 pp), reproducible from M2 SWE row: priv same-model endpoint R8=0, contam insufficient, horizon cross-model (GPT-5.2 R4→R9) excluded per §1.8 → max(0,0,0)=0 → usable. `discount-heavily`: GPQA, MMLU. `noise`: SWE×Opus cross-model, SWE×GPT-5, GSM8K, MATH. Each classification cites its driving M2 components in-row.
4. Worked example M1→M2→discount for one named benchmark — PASS. §3 walks GSM8K fully: R22 (H_ref=95) → M2 components (c from R23, horizon from R24) → P_contam=[6.4,10.2], P_horizon=[28.5,62.7] → max → 28.5–62.7 → noise. §3b additionally walks the Opus usable row R6/R8/R3/(R4→R9 excluded) → 0–0 → usable.

## Notes
The artifact reuses the same benchmark (SWE-bench × Opus 4.1) in two rows under two disclosed scopings (rank 1 usable / rank 4 noise). This is acceptable: each row independently carries range+class+confidence, the scoping is disclosed in the model-family column and notes, §1.8 grounds the scoping in M1 G8 model-matching, monotonicity holds across all 7 rows, and the usable classification is mechanically reproducible by applying §1.8 then §1.4 to the cited M2 cells. The artifact is transparent (§5.2, §5.4) that the usable verdict is conditional on the scoping and does not soften the overall "almost everything needs a large discount" conclusion — this honesty satisfies rather than undermines the criterion.
