# Verdict: FAIL
Artifact: /home/user/stuck-problems-agent/problems/ai-benchmark-deployment-gap/artifacts/2026-06-28-m3-procurement-discount-attempt1.md
Date: 2026-06-28

## Checks
- [x] Sourced — every quantitative cell cites an M2 component → M1 row (R-numbers) or is flagged `[PROXY]`/`[CROSS-MODEL]`; contamination conversions show `c × H_ref` explicitly. No bare numbers.
- [x] Current — all referenced models/benchmarks are 2025–2026 frontier (GPT-5.2, Opus 4.1, SWE-bench Pro, GSM-NoOp); nothing stale is presented as current state.
- [x] Adversarial — §5 has 10 substantive limitations that engage real failure modes (max-vs-sum understatement, the scoping fork that flips the one usable row to noise, the measured-zero floor-effect risk, H_ref sensitivity). Not boilerplate.
- [x] Format — table carries benchmark | model family | discount [low–high pp] | classification | dominant driver | confidence | components used; preceded by the combination rule (§1.1) and followed by a worked example (§3). Matches the spec format.
- [✗] Criteria — see per-criterion lines below; criterion 3 fails.
  - DC1 (rule explicit & reproducible): PASS — §1.1 formula reproduced independently for GSM8K (0.067×95=6.4, 0.107×95=10.2; max(6.4,0,28.5)=28.5, max(10.2,0,62.7)=62.7 → 28.5–62.7 pp, matches the table).
  - DC2 (every row has band+class+confidence; monotonic ranking): PASS — all 7 rows complete; Discount_high = 0,14.9,33,49.9,49.9,62.7,96 is non-decreasing; the 49.9 tie broken by ascending Discount_low (0 before 26.1). Verified.
  - DC3 (≥1 usable AND ≥1 noise/discount-heavily, each citing M2 components): **FAIL** — the only `usable` row (rank 1, SWE-bench Pro × Opus 4.1) is built on a public→private gap claimed as "17.8% public → 17.8% held-out, Δ=0.0 pp (R8)". This "measured zero" is false (see spot-check) and is the load-bearing driver of the usable classification, its dominant-driver label, and its confidence cap.
  - DC4 (worked example M1→M2→discount): PASS — §3 walks GSM8K from R22/R23/R24 through M2 components to 28.5–62.7 pp and the noise class with full arithmetic.
- [✗] Spot-check — Sampled 3 claims. (a) GSM8K worked-example arithmetic: reproduces correctly. (b) GPQA contamination 0.005–0.166 × 90 = 0.5–14.9 pp: arithmetic correct. (c) **SWE-bench Pro × Opus 4.1 public→private gap (rank 1, R8): FAILS.** The artifact states "17.8% public → 17.8% commercial held-out, Δ=0.0 pp." Scale Labs' SWE-bench Pro leaderboard reports Opus 4.1 at 23.1% public and explicitly notes it "decreases from 22.7% to 17.8% resolution" on the private/commercial set — a ~5 pp drop, not zero. The artifact reused the private score (17.8%) as the public score, manufacturing a measured zero. The cited component does not support the claim.

## Critique (FAIL only)

The artifact is structurally sound and three of four done-criteria pass cleanly. It fails on one specific, fixable error that happens to sit under the most load-bearing claim in the retry.

1. **Fix the rank-1 public→private gap — it is factually wrong, not just a scoping choice.** You state Opus 4.1 is "17.8% public → 17.8% commercial held-out, Δ=0.0 pp (R8)". The real SWE-bench Pro numbers for Opus 4.1 are ~22.7–23.1% public → 17.8% private/commercial (Scale Labs leaderboard). The public→private gap is ~5 pp, not 0. You appear to have used the private score as both endpoints. Re-derive `Δpp_priv` from the correct public endpoint. Note this likely *keeps* the row `usable` (~5 pp ≤ 10), but it destroys the "measured zero" narrative, the §1.5/§1.7 "measured-zero" special-case reasoning, the §5.3 floor-effect discussion, and the dominant-driver justification — all of which must be rewritten around a ~5 pp gap, not 0. Verify the public number against M1 before using it; if M1 itself records 17.8→17.8, the error is upstream and you must flag that the M1/M2 cell contradicts the published leaderboard rather than propagate it.

2. **Resolve the SWE-bench Pro vs SWE-bench Verified benchmark-identity confusion.** Rank 1 is labeled "SWE-bench Pro" and ranks 4–5 "SWE-bench Verified," yet the same R8 datapoint (17.8→17.8) is cited as the priv channel for *both* rank 1 (Pro) and rank 4 (Verified), and the §1.7 H_ref table lists Verified at 72.80 (R4) and Pro at 17.8 (R6) as distinct benchmarks. A 17.8% headline is a SWE-bench Pro number; a 72.80% headline is a SWE-bench Verified number — they cannot both index the same R8 public→private measurement. Pin each SWE row to exactly one benchmark and make sure its contamination (R3), priv (R8), and horizon (R4→R9) cells all belong to that same benchmark. As written, the usable row mixes Pro evidence (R6, R8) with Verified evidence (R3, R4→R9), so DC3's requirement that the classification "cite the M2 components that drove it" is not cleanly met even apart from the numeric error.

3. Once (1) and (2) are corrected, re-run the monotonic sort (rank 1's Discount_high will move from 0 to ~5 but should still lead) and re-verify the §2 monotonicity line and the §4 coverage check reflect the corrected numbers. Do not weaken the max rule or introduce new external numbers; only correct the public-set endpoint and the benchmark labels.
