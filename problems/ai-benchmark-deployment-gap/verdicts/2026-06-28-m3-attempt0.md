# Verdict: FAIL
Artifact: /home/user/stuck-problems-agent/problems/ai-benchmark-deployment-gap/artifacts/2026-06-28-m3-procurement-discount.md
Date: 2026-06-28

## Checks

- [x] Sourced — Every discount is derived from M2 component ranges (themselves M1 rows R1–R29) plus a cited `H_ref` (also an M1 row); no new external number is introduced. Spot arithmetic on two rows reproduces the cited values.
- [x] Current — Underlying evidence (GSM-Symbolic/GSM-NoOp, MMLU-Pro, SWE deltas, frontier headlines GPT-5.2/GPT-4o) is the contemporary 2025–2026 benchmark literature; nothing stale is presented as the current state.
- [x] Adversarial — §5 has eight substantive, non-boilerplate limitations: the max-vs-sum lower-bound choice, the contestable empty `usable` tier, `H_ref` sensitivity, the n=1 SWE horizon dependence, MATH's near-uninformative band, threshold-boundary sensitivity, confidence-vs-importance, and inherited model-version heterogeneity.
- [x] Format — Markdown analysis artifact with explicit combination rule, ranked per-benchmark discount table, worked example, and coverage check; matches the M3 deliverable format.

- Criterion 1 (rule explicit & reproducible) — PASS. §1.1 states the formula (`P_contam = c×H_ref`, pass-through pp channels, per-endpoint `max`), units, and range propagation. Independently applied to the MMLU row (not the worked example): c=0.166–0.186, H_ref=88 → P_contam=[14.6,16.4]; P_priv=[16,33]; P_horizon=0; max-low=16, max-high=33 → 16–33 pp, exactly matching the table. GSM8K also reproduces (28.5–62.7).
- Criterion 2 (every row has discount + classification + confidence; monotonic ranking) — PASS. All six rows carry a [low–high pp] band, a classification, and a confidence label; sorted ascending by `Discount_high` (§1.6), ties by `Discount_low` (GPQA 14.9 < MMLU 33 < SWE-Opus 49.9 = SWE-GPT5 49.9 ordered by low 0<26.1 < GSM8K 62.7 < MATH 96). Monotonic and consistent.
- Criterion 3 (≥1 'noise'/'discount-heavily' AND ≥1 'usable', each citing M2 components) — **FAIL**. The criterion as written requires at least one benchmark classified **'usable'**. The table contains zero 'usable' rows: GPQA (high 14.9 pp) and MMLU clear the 10-pp bar into 'discount-heavily', and SWE×2/GSM8K/MATH are 'noise'. The artifact itself states "no row reached `usable`" and "the `usable` band defined and shown empty." The conjunction ('AND at least one usable') is therefore not satisfied. (The M2-component citation requirement is met; the missing element is the 'usable' classification itself.)
- Criterion 4 (worked example M1→M2→discount) — PASS. §3 walks GSM8K end-to-end: R22 → H_ref=95.0; R23 → c=0.067–0.107; R24 → horizon 28.5–62.7; Step A pp conversion; Step B max → 28.5–62.7 pp → noise. Full arithmetic shown for one named benchmark.

- [x] Spot-check — Sampled (a) GSM8K contamination/horizon (R23 GSM-Symbolic, R24 GSM-NoOp) and (b) the MMLU combination arithmetic. The GSM-NoOp source confirms GPT-4o ≈94.9→63.1 and Phi-3-mini catastrophic drop; GSM-Symbolic confirms the cross-model contamination interpretation. MMLU table number reproduced from M2 row independently. Citations support their claims.

## Critique (FAIL only)

Three of four done-criteria pass and the combination rule is genuinely reproducible — the only failure is criterion 3, judged exactly as written. The criterion demands a benchmark classified **'usable'** AND a benchmark classified 'noise' or 'discount-heavily'. Your table produces none in the 'usable' (≤10 pp high) tier and reports that as an honest finding. An honest empty tier does not satisfy a criterion that literally requires at least one 'usable' classification.

To pass on retry, one of these must hold and be defensible:

1. Surface at least one benchmark that lands in 'usable' under the rule. The nearest candidate is GPQA: its `Discount_high` is 14.9 pp solely because the contamination high end uses a `15/90≈0.166` whole-benchmark *ceiling* that M2 itself flags as "indirect, subset-only… true value sits near the low end" (M1 G6). If, per M2's own caveat, the contamination *high* endpoint is the subset-correct value (~3% of items affected, i.e. `0.03×15/90×90 ≈ 0.5 pp`, or otherwise ≤10 pp on a defensible same-benchmark basis), GPQA's `Discount_high` falls below the 10-pp line and classifies 'usable'. Make that derivation explicit and reproducible from the M2 GPQA row, and cite the M2 components driving it (as criterion 3 also requires). This is the cleanest fix and is consistent with your own §5 note that GPQA is "nearly flat" on same-model frontier evidence.

2. If you maintain that no benchmark is 'usable', then the table cannot satisfy criterion 3 as written — do not rely on "defined but empty" language to stand in for an actual 'usable' row. Either re-examine the threshold/derivation so a genuinely usable benchmark appears, or escalate the conflict between your conservative finding and the criterion rather than declaring the criterion met.

Keep everything else intact: §1.1 rule, the ranked table, the GSM8K worked example, and §5 limitations all meet their criteria. Only the presence of a defensible, M2-cited 'usable' classification is missing.

VERDICT: FAIL
