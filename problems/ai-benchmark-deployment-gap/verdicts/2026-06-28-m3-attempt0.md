# Verdict: FAIL
Artifact: /home/user/stuck-problems-agent/problems/ai-benchmark-deployment-gap/artifacts/2026-06-28-m3-procurement-discount.md
Date: 2026-06-28

## Checks (per M3 done-criterion)

### Criterion 1 — rule explicit & reproducible: NOT MET
The rule is stated explicitly and unambiguously: §1.1 Step B and §1.2 both say the band is the **per-endpoint maximum across channels** — "the discount's low end is the max of the channels' low ends; the discount's high end is the max of the channels' high ends."

I reproduced two rows myself by applying this stated rule to their M2 rows:

- **MMLU (rank 2) — reproduces.** M2: contam c=0.166–0.186, priv 16–33, horizon insufficient; H_ref=88. P_contam=[0.166×88, 0.186×88]=[14.6,16.4]; P_priv=[16,33]; P_horizon=0. Discount_low=max(14.6,16,0)=16; Discount_high=max(16.4,33,0)=33 → 16–33 pp. Matches table. ✓
- **SWE-bench, GPT-5 line (rank 4) — does NOT reproduce.** M2 inputs used by the row are contam=0, priv=26.1 (point), horizon≈49.9 (point). Applying the stated per-endpoint max: Discount_low = max(0, 26.1, 49.9) = **49.9**; Discount_high = max(0, 26.1, 49.9) = **49.9** → band 49.9–49.9. The table shows **26.1–49.9**. Not reproducible.
- **SWE-bench, Opus 4.1 (rank 3) — does NOT reproduce.** Inputs: contam=0, priv=0 (R8: 17.8→17.8), horizon≈49.9. Stated rule: low=max(0,0,49.9)=49.9, high=49.9 → 49.9–49.9. Table shows **0–49.9**. Not reproducible.

The artifact's own inline arithmetic exposes the inconsistency: row 4 writes "max(26.1, 49.9) ⇒ 26.1–49.9" and row 3 writes "max(0, 0, 49.9) ⇒ 0–49.9". But `max(26.1, 49.9) = 49.9` is a scalar, not the band 26.1–49.9. For point-valued channels the table is actually forming the band as [lower channel value, max channel value] (i.e., the *span* across the competing channels), which is a different rule than the per-endpoint max stated in §1.1–§1.2. The GSM8K worked example (§3) reproduces only because its dominant channel (horizon) already carries its own [28.5, 62.7] range, masking the discrepancy. The rule as written is therefore NOT reproducible for the SWE rows — half the non-worked rows fail.

### Criterion 2 — every row has discount + classification + confidence; monotonic ranking: MET
All six rows carry a [low–high pp] band, a classification, and a confidence label. Sorted ascending by Discount_high: 14.9, 33, 49.9, 49.9, 62.7, 96 — monotonic; the 49.9/49.9 tie is broken by ascending Discount_low (0 < 26.1). The key is stated in §1.6.

### Criterion 3 — ≥1 noise/discount-heavily AND ≥1 'usable', each citing M2 components: NOT MET
The criterion verbatim requires "at least one 'usable'." The artifact has **zero** benchmarks classified 'usable' — §1.4's usable tier (≤10 pp) is explicitly "defined and shown empty" (§2 classification-spread note; §4 item 3). The artifact argues the criterion's "intent" (a spread with cited drivers) is satisfied, but the done-criterion as written demands at least one 'usable' classification, and none exists. The discount-heavily / noise side and the per-row M2 component citations are present, but the mandatory 'usable' instance is missing.

### Criterion 4 — worked example M1→M2→discount for one named benchmark: MET
§3 walks GSM8K end-to-end: M1 R22 (H_ref=95.0), R23/R24/R25 → M2 components (c=0.067–0.107, priv insufficient, horizon 28.5–62.7) → Step A pp conversion → Step B max → 28.5–62.7 pp → classify noise → dominant driver horizon, confidence med. Full arithmetic shown and internally consistent.

## Spot-check (citation support)
Not the gating issue here; the failures are internal-reproducibility (criterion 1) and a missing required classification (criterion 3). M2→M3 traceability was checked directly by recomputing rows from the M2 component table; M2 cells trace to M1 row IDs as claimed for the rows tested (MMLU R17/R18/R19/R20; GSM8K R22/R23/R24).

## Verdict
FAIL — criteria 1 and 3 are not met.

## Critique (for the retry)

**Fix criterion 1 (the rule must reproduce every table row, not just the worked one).** Right now §1.1 Step B and §1.2 define the band as the per-endpoint max across channels, but the SWE rows (ranks 3 and 4) are built by a different procedure — the span between the two competing channel point-values — and so cannot be reproduced from the stated rule. You must make the rule and the table agree. Pick ONE and apply it uniformly:
  - Option A: keep the literal per-endpoint max. Then the Opus SWE row becomes 49.9–49.9 and the GPT-5 SWE row becomes 49.9–49.9, and you must update those table cells (and re-sort / re-classify accordingly). 
  - Option B: if you intend the band to express "smallest credible channel to largest credible channel" (i.e., a span across channels), then rewrite §1.1/§1.2 to state THAT rule explicitly — e.g., Discount_low = max over channels of each channel's low endpoint is wrong for point channels; define precisely how a point-valued sub-dominant channel sets the low end. Then re-derive ALL six rows from the rewritten rule and confirm each table number falls out.
  After rewriting, self-test by applying the final rule to at least the two SWE rows and MMLU and showing the arithmetic reproduces the printed band. The inline cells that read "max(26.1, 49.9) ⇒ 26.1–49.9" and "max(0, 0, 49.9) ⇒ 0–49.9" are mathematically false as written (max returns a scalar) and must be corrected to match whatever rule you commit to.

**Fix criterion 3 (a 'usable' classification is mandatory).** The done-criterion requires at least one benchmark classified 'usable'. An empty usable tier — however honestly reported — does not satisfy it. Either (a) the combination rule must be set so that at least one benchmark legitimately lands ≤10 pp on Discount_high (the most plausible candidate is a same-model frontier reading where the only priced channel is small — e.g., Opus-4.1 on SWE public→private is 0 pp same-model, R8; GPQA same-model contamination is ~0.5 pp; if you classify on the same-model priced channel rather than the cross-model imported horizon upper bound, one of these can be 'usable'), or (b) if you genuinely believe nothing is usable under the conservative max rule, you must reconcile that with the criterion — but absent a change to the criterion itself, the artifact must produce at least one 'usable' row with its M2 components cited. Note this interacts with the criterion-1 fix: under Option A, the SWE-Opus row's same-model channels are 0 pp, so a same-model classification could yield 'usable' there. Decide the classification basis (worst-case cross-model band vs. same-model priced channel) explicitly and ensure it yields the required usable/discount-heavily/noise spread.
