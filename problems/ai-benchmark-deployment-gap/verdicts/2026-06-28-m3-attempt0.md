# Verdict: FAIL
Artifact: /home/user/stuck-problems-agent/problems/ai-benchmark-deployment-gap/artifacts/2026-06-28-m3-procurement-discount.md
Date: 2026-06-28

## Checks
- [x] Sourced — Every discount cell traces to M2 components and M1 rows (table col 8, §1.7 H_ref table). No bare quantitative claim lacks a cited row; the artifact introduces no new external number and labels MATH's proxy figures `[PROXY]`.
- [x] Current — Sources are 2024–2026 (GSM-Symbolic/GSM-NoOp 2024-2025 ICLR, frontier headlines GPT-5.2/Opus-4.1 2026). Cross-model/proxy provenance is flagged, not passed off as current frontier state. Nothing stale presented as current.
- [x] Adversarial — §5 has 8 substantive limitations engaging real counter-evidence (max-vs-sum understatement, empty `usable` tier as a contestable claim, H_ref sensitivity, SWE n=1 fragility, MATH near-uninformative band, threshold sensitivity). Not boilerplate.
- [x] Format — Ranked table present with all required columns (benchmark, model family, discount [low–high pp], classification, dominant driver, confidence, components used), preceded by combination rule (§1.1) and followed by worked example (§3). Most-trustworthy first via ascending Discount_high.
- ✗ Criteria — see below; one done-criterion unmet.
  - Rule explicit & reproducible: PASS. Independently applied §1.1 to MMLU (row 2): contam [0.166×88, 0.186×88]=[14.6,16.4], priv [16,33], horizon 0 → Discount_low=max(14.6,16,0)=16, Discount_high=max(16.4,33,0)=33 → 16–33 pp, matches table. Also verified GPQA (0.005–0.166×90 → 0.5–14.9) and GSM8K (worked example, 28.5–62.7). PASS.
  - Every row has discount+classification+confidence, monotonic ranking: PASS. All 6 rows carry a band, class, confidence; sorted ascending by Discount_high (0.5–14.9 → 16–33 → ... → 16–96 by high end 14.9,33,49.9,49.9,62.7,96), ties by Discount_low. Monotonic and stated (§1.6).
  - ≥1 noise/discount-heavily AND ≥1 usable, each citing M2 components: FAIL. discount-heavily (GPQA, MMLU) and noise (SWE×2, GSM8K, MATH) are present and cite components. But ZERO benchmarks are classified "usable." The criterion explicitly conjoins "AND at least one 'usable'." The artifact (§2 spread note, §4 item 3) reinterprets this as the criterion's "intent" and reports the usable tier as empty. As written and checked individually, the conjunction is not satisfied.
  - Worked example M1→M2→discount: PASS. §3 walks GSM8K from R22/R23/R24/R25 → M2 components → 28.5–62.7 pp → noise with full arithmetic.
- [x] Spot-check — Sampled GSM-NoOp (R24) and GSM-Symbolic (R23) external sources. GSM-NoOp: GPT-4o drops 94.9%→~63% (artifact: 94.9−63.1=31.8 ✓); overall drops up to ~65% (artifact horizon band high 62.7 ✓). GSM-Symbolic contamination framing confirmed. Citations accurately represent the underlying studies.

## Critique (FAIL only)
The combination rule, reproducibility, ranking, worked example, sourcing, and adversarial section are all solid — do not rework them. The single blocking issue is done-criterion 3, which requires at least one benchmark classified "usable" in addition to at least one "noise"/"discount-heavily". Your table classifies none as "usable"; you instead argue the empty tier is itself the finding. That may be substantively defensible, but it does not satisfy the criterion as the spec wrote it, and the evaluator must check criteria literally.

To pass, do exactly one of the following on the retry:

1. Preferred — produce at least one benchmark/model-family row that legitimately lands in "usable" (Discount_high ≤ 10 pp) under the same uniform rule, citing the M2 components that drive it. Candidates where same-model frontier evidence is near-flat already exist in your own data and Limitations §5.2: GPQA's same-model/direct contamination evidence and Opus-4.1-on-SWE public→private (R8, 17.8→17.8, Δpp_priv=0). If you can construct a row whose priced channels all sit ≤10 pp (e.g., an Opus-4.1 SWE row scoped to the *same-model* priced channels without importing the cross-model n=1 horizon figure into its high end, or a GPQA row scoped to direct rather than subset-only contamination), classify it "usable" with the components cited and the scoping justified. Keep the conservative max rule; just be explicit about which channels are in-scope for that row and why, so the "usable" verdict is reproducible from the cited M2 cells.

2. If you genuinely believe no row can honestly reach "usable" under any defensible scoping, then the spec's criterion conflicts with the evidence — but you may not resolve that by reinterpreting the criterion inside the artifact. The artifact cannot self-grant the criterion. Either deliver a real "usable" row (option 1) or the milestone cannot pass on these criteria.

Everything else is ready; this is the only gap.
